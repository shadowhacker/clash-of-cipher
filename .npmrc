# Set platform architecture to 'linux' to ensure correct builds on hosting platforms
node-linker=hoisted

# Tell npm to ignore optional dependencies, which helps with the Rollup platform issue
ignore-optional=true

# Force using Node.js version that works with the current setup
engine-strict=true

# Increase network timeout for better reliability
network-timeout=100000 